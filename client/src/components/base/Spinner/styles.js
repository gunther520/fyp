const defaultStyle = "animate-spin rounded-full border-2 border-t-blue-500 border-r-transparent border-b-transparent border-l-transparent";

const styles = {
  default: `${defaultStyle} h-4 w-4`,
  small: `${defaultStyle} h-4 w-4`,
  large: `${defaultStyle} h-15 w-15`,
};

export default styles;
